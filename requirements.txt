# 基礎科學運算
numpy>=1.24.0
pandas>=2.1.0
scipy>=1.11.0

# RDKit (ECFP 計算)
# rdkit-pypi provides RDKit via pip on some platforms. It is not available
# as a pip wheel on Windows. To avoid pip install failures on Windows, only
# install rdkit-pypi on non-Windows platforms; Windows users should install
# RDKit via conda: conda install -c rdkit rdkit
rdkit-pypi>=2023.9.1; sys_platform != "win32"

# 機器學習
scikit-learn>=1.3.0
xgboost>=1.7.6

# SHAP 分析
shap>=0.42.1

# 繪圖
matplotlib>=3.8.0
seaborn>=0.12.2

# Optional: 儲存圖片 / PNG
pillow>=10.0.0

# Pipeline / utility
joblib>=1.3.0
